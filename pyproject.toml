[project]
name = "sc-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.2.10",
    "langchain-community>=0.4.1",
    "langgraph>=1.0.10",
    "ruff>=0.15.4",
]

[tool.ruff]
line-length = 120
